#include "main.h"

/**
 * add - a function that adds two numbers
 * @one - first number input
 * @two - second number input
 * Return: one + two
 *
 */
int add((one) + (two))
{
	return: (one + two);
}	
