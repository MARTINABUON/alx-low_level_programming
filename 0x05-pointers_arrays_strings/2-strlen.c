#include "main.h"

/**
 *_strlen - returns the length of the function
 *@s: - pointer to the length of the function
 * Return: int( lenght of the string)
 */
int _strlen(char *s)
{
	int len = 0;

	while (*s != '\0')
	{
		len++
		s+
	}
	return (len);
}


