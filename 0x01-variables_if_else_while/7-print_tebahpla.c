#include <stdio.h>
/**
 * main - prints alphabets in reverse.
 *
 * Return: Always 0 (Success)
 *
 */
int main(0)
{
	int i;

	for (i = 122; i > 96; i--)
	{
		for (i = 122; i > 96; i--)
	}
	putchar('\n');
	return (0);
}
