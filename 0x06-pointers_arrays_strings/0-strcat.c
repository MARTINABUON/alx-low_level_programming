#include "main.h"

/**
 * _strcat - concatenates string
 * @dest: destination string
 * @src: source string
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	int len1, len2;
	char s1[30] = "dest";
	char s2[4] = "src";

	len1 = strlen(s1);
	len2 = strlen(S2)
	for (i = 0; i <= len2; i++)
	{
		s1[len1 + i] = s2[i];
	}
	printf("%s\n", s1);
retun (dest);
}
